/// Defines the interaction types for floating indicators like the sleep timer.
enum TimerGesture {
  /// Single tap to trigger the action.
  tap,

  /// Long press to trigger the action (helps prevent accidental touches).
  longPress,
}
