/// Centralised UI constants for the Nix application.
abstract final class NixConstants {
  /// The height of the MiniPlayer when collapsed.
  static const double kMiniPlayerHeight = 82.0;

  /// The height of the standard NavigationBar.
  static const double kNavBarHeight = 80.0;

  /// A comfort margin to add at the end of lists.
  /// Set to 0 because NavigationScreen already adds safety padding.
  static const double kBottomPadding = 0.0;
}
